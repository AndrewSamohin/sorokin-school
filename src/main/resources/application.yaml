spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    drive-class-name: org.postgresql.Driver

    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update

account:
  default-amount: 500
  transfer-commission: 0.1